<mvc:View controllerName="portaleamministrativo.controller.Detail"
  xmlns:mvc="sap.ui.core.mvc"
  xmlns="sap.m"
  xmlns:c="sap.ui.core"
  xmlns:f="sap.f">
  <f:DynamicPage showFooter="true"
    class="sapUiNoContentPadding">
    <f:header>
      <f:DynamicPageHeader pinnable="false">
        <f:content>
          <!-- <c:Fragment fragmentName="portaleamministrativo.view.fragment.detail.Funzionale.Header" type="XML" /> -->
        </f:content>
      </f:DynamicPageHeader>
    </f:header>
    <f:content>
      <IconTabBar>
        <items>
          <IconTabFilter key="info"
            text="{i18n>labelInfo}">
            <c:Fragment fragmentName="portaleamministrativo.view.fragment.detail.Funzionale.Header"
              type="XML" />
            <c:Fragment fragmentName="portaleamministrativo.view.fragment.detail.Funzionale.Info"
              type="XML" />
          </IconTabFilter>
          <IconTabFilter key="attachments"
            text="{i18n>labelAttachments}">
            <c:Fragment fragmentName="portaleamministrativo.view.fragment.detail.TableAttachments"
              type="XML" />
          </IconTabFilter>
          <IconTabFilter key="comments"
            text="{i18n>labelComments}">
            <c:Fragment fragmentName="portaleamministrativo.view.fragment.detail.TableAttachments"
              type="XML" />
          </IconTabFilter>
        </items>
      </IconTabBar>
    </f:content>
    <f:footer>
      <OverflowToolbar>
        <Button type="Back"
          text="{i18n>labelBack}"
          press="onBack" />
        <ToolbarSpacer />
        <Button type="Emphasized"
          text="{i18n>labelSend}"
          press="onSend" />

      </OverflowToolbar>
    </f:footer>
  </f:DynamicPage>
</mvc:View>